# Schedules：定时任务

Schedule 让 Agent 按固定节奏启动，而不是等待用户消息。适合日报、数据同步、清理、心跳检查等周期性工作。

## 要点

- 文件放在 `agent/schedules/` 下。
- 每个文件定义一个时间表达式和要执行的任务。
- 可以是简单 prompt，也可以是 handler。
- 开发环境通常手动触发，生产环境交给宿主定时系统运行。

## 工程建议

定时任务常常涉及外部副作用，例如发消息、同步数据、调用 API。正式场景需要做好权限、幂等、失败重试和日志观测。
