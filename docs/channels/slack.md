# Slack Channel

这一页对应官方 Slack channel 文档。Slack channel 用来把 eve agent 接入 Slack，让团队成员可以在 Slack 中和 Agent 对话。

## 要点

- 把 Slack 消息转换成 eve session 输入。
- 把 Agent 回复渲染回 Slack。
- 可以承载审批、提问和按钮选择等交互。
- 生产环境需要处理签名校验、权限范围和用户身份映射。
